[tool.poetry]
name = "Argument-Graph"
version = "0.1.0"
description = ""
authors = ["Mirko Lenz <mirko@lenz.am>"]

[tool.poetry.dependencies]
python = "^3.7"
pygraphviz = "^1.5"
networkx = "^2.4"
spacy = "^2.2"

[tool.poetry.dev-dependencies]
pytest = "^3.0"
black = {version = "^18.3-alpha.0", allows-prereleases = true}

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
